{
  "name": "slz-gui",
  "version": "0.9.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": "20.19.3"
  },
  "scripts": {
    "test": "mocha unit-tests/",
    "coverage": "npx nyc npm run test",
    "build": "cd client && npm run build && cp -r build ../",
    "clean": "npm install && cd client && npm install",
    "install-build": "npm run install && npm run build",
    "md": "node lib/docs-as-markdown",
    "changelog": "node lib/changelog-as-markdown",
    "deploy": "./deploy.sh",
    "compatibility-tests": "./compatibility.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lazy-z": "1.12.1"
  },
  "devDependencies": {
    "chai": "5.2.0",
    "mocha": "10.8.2",
    "nyc": "17.1.0"
  },
  "overrides": {
    "express": "4.21.2",
    "nth-check": "2.1.1",
    "postcss": "8.5.6",
    "webpack-dev-middleware": "7.4.2"
  }
}
